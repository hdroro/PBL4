const routes = {
    home: '/',
    call: '/call/:nickname',
    profile: '/profile/:nickname',
    search: '/search',
    messages: '/messages',
    matching: '/matching',
    setting: '/setting',
};

export default routes;
