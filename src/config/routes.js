const routes = {
    home: '/',
    call: '/call/:nickname',
    profile: '/:nickname',
    search: '/search',
    messages: '/messages',
    matching: '/matching',
    setting: '/setting',
};

export default routes;
